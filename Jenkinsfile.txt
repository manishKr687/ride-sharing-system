pipeline {
    agent any

    tools {
        maven 'Maven 3.9.6'   // Match the Maven name you configured
        jdk 'JDK17'           // Match the JDK name you configured
    }

    stages {
        stage('Checkout') {
            steps {
                git 'https://github.com/manishKr687/ride-sharing-system.git'
            }
        }
        stage('Build') {
            steps {
                sh 'mvn clean install'
            }
        }
        stage('Test') {
            steps {
                sh 'mvn test'
            }
        }
    }
}
